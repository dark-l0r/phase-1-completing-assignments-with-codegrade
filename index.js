// Write your code in this file!
const currentUser = 'Ronald'

const welcomeMessage = `Welcome to Flatbook,${currentUser.slice()}!`

const shortGreeting = `Welcome, ${currentUser.slice()}`
 
console.log(welcomeMessage)